import { PrintDirective } from './print.directive';

describe('PrintDirective', () => {
  it('should create an instance', () => {
    const directive = new PrintDirective();
    expect(directive).toBeTruthy();
  });
});
