import { ProfilSpecialPipe } from './profil-special.pipe';

describe('ProfilSpecialPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfilSpecialPipe();
    expect(pipe).toBeTruthy();
  });
});
